{
  "name": "SIAKAD",
  "private": true,
  "description": "SIAKAD — Sistem Informasi Akademik (Modular Monolith)",
  "scripts": {
    "dev": "npm run dev:be & npm run dev:fe & wait",
    "dev:be": "cd backend && npm run dev",
    "dev:fe": "cd frontend && npm run dev",
    "build:fe": "cd frontend && npm run build",
    "build:be": "cd backend && npm run build",
    "clean": "rm -rf node_modules && cd backend && rm -rf node_modules && cd ../frontend && rm -rf node_modules"
  }
}
